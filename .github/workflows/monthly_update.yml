name: Monthly Data Update

on:
  schedule:
    - cron: '12 18 18 2 *' # Runs at 10:50 AM MST on February 18th, 2025
  # - cron: '0 10 1 * *' # Runs at 3 AM (MST) on the 1st of every month
  workflow_dispatch: # Allows manual triggering from GitHub Actions UI

jobs:
  update-data:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          pip3 install --no-cache-dir fastapi uvicorn[standard] psycopg2-binary shapely numpy osmnx networkx scikit-learn geopandas python-multipart orjson

      - name: Grant execute permissions to the script
        run: chmod +x backend/data/generate_seed_data.py ec2/seed.sh

      - name: Run the update script
        run: |
          python3 ./backend/data/generate_seed_data.py && bash ./ec2/seed.sh
