# build frontend app locally
cd frontend
rm -r node_modules && rm -r dist
npm install
npm run build

# overwrite dist directoty to frontend/dist with ssh
scp -i walkernest-key-pair.pem -r dist ec2-user@18.216.217.247:/home/ec2-user/walkernest/

# run docker-compose.yml on EC2 instance via ssh
export FRONTEND_DOCKERFILE=frontend/Dockerfile.prod && export NODE_ENV=production && docker-compose up --build